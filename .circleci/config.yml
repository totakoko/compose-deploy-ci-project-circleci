version: 2

jobs:
  deploy:
    docker:
      - image: totakoko/compose-deploy-ci:4acbc08741d63d58fae020b42475c0ea0f1146f1
    steps:
      - checkout
      - run:
          command: compose-deploy deploy

workflows:
  version: 2
  commit:
    jobs:
      - deploy:
          requires: []