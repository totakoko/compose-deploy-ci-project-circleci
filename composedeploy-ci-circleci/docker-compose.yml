version: '3'

services:
  server:
    image: alpine
    restart: unless-stopped
    command: ash -c 'while true ; do echo -e "HTTP/1.1 200 OK\n\n$${CONTENT:-It works!}" | nc -l -p 5000; done'
    env_file:
      - secret.env
    environment:
      - CONTENT=adec1d447466b96ee8fd65574310fdc78be8bdc1-2022-09-20T10:03:32+00:00
    ports:
      - 8500:5000
