version: '3'

services:
  server:
    image: alpine
    restart: unless-stopped
    command: ash -c 'while true ; do echo -e "HTTP/1.1 200 OK\n\n$${CONTENT:-It works!}" | nc -l -p 5000; done'
    env_file:
      - secret.env
    environment:
      - CONTENT=c975b27293ff47d1aae0d7b087edc6b143ec3755-2022-09-20T09:28:03+00:00
    ports:
      - 8500:5000
